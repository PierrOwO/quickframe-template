<?php

namespace App\Middleware;

class ClassName
{
    public function handle($request, $next)
    {
        // before
        $response = $next($request);
        // after

        return $response;
    }
}